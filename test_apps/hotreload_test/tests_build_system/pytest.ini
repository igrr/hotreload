[pytest]
# Build system tests - no hardware or QEMU needed
# This directory is separate to avoid pytest-embedded plugin interference
testpaths = .
python_files = test_*.py
python_functions = test_*
# Disable pytest-embedded and idf-ci plugins for pure build system tests
addopts = -v -p no:embedded -p no:idf-ci
